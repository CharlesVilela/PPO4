
<a href="/usuario/create" class="btn btn-primary">
    Create
</a>

<a href="/login/logar" class="btn btn-primary">
    login
</a>



<div class="row">
    {{#each listar}}
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <p>{{nomeUsuario}}</p>
                <p>{{email}}</p>
                <p>{{senha}}</p>
            </div>
            <div class="card-footer d-flx justify-content-between align-items-center">
                <a href="/usuario/delete/{{_id}}" class="btn btn-danger">
                    Delete
                </a>
                <a href="/usuario/update/{{_id}}" class="btn btn-secondary">
                    Edit
                </a>
                
            </div>
        </div>
    </div>

    {{else}}

    {{/each}}
</div>